version: '3'
services:
  mongo:
    image: mongo:3.6
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - ./volume/mongo/:/data/db
    networks:
      - baas-challenge
networks:
    baas-challenge:
      driver: bridge